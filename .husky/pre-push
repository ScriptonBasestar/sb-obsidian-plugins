#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run linting (unified with make lint and pre-commit)
echo "Running lint checks..."
pnpm lint

# Run tests if they exist
echo "Running tests..."
pnpm test || echo "⚠️  Some tests failed but continuing..."

echo "✅ Pre-push checks completed"